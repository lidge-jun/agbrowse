# Docs-only plan audit

## Round 1

Focused pre-scan: 4 files, 142 tests passed.

Verdict: `GO-WITH-FIXES (blockers=2)`.

1. The roadmap allowed inert submenu templates, which could not prove top-shell to sibling-portal reachability.
2. The release phase did not name a safe strategy for the six current `dev` to `main` merge conflicts.

## Fold-back

- `010_picker_contract.md` now requires executable production-locator activation plus three independent mutation REDs: old root-only selector, removed sibling menu search, removed exact submenu trigger.
- `030_release_and_deploy.md` now uses a fresh main worktree, computes the picker prerequisite closure, excludes attachment/Gemini conflicts, and stops UNSAFE if the scoped chain cannot be separated.

## Round 2

Same reviewer verified both blocker closures.

Final verdict: `PASS`.
