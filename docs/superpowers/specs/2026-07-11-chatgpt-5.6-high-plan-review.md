# ChatGPT 5.6 High Plan Review

**Reviewed:** 2026-07-11

**Model selection:** GPT-5.6 Sol, Thinking, High

**Web-AI session:** `01KX8N2K4W2Z45S4CC4YXDN5MW`

**Conversation:** `https://chatgpt.com/c/6a5241eb-d838-83e8-9b50-24a89081533f`

**Verdict:** REJECT before revision

## Blocking Findings

1. The fresh-process helper imported `getSession` from the wrong module.
2. The Perplexity URL guard inspected a WHATWG-normalized path and could miss
   raw or encoded traversal.
3. Two recovery navigation paths were unguarded, and a broad catch could
   swallow the typed guard error.
4. Omitted effort still forced Thinking control lookup.
5. `perplexityStatusWebAi()` had no direct behavioral Red test or guaranteed
   implementation step.
6. Clean-checkout commands used a global `agbrowse` instead of the
   repository-local binary after `npm ci`.

## Major Findings

- Use the existing `runBoundSendOrQuery()` helper and include `render`
  dispatch.
- Validate normalized CLI input before browser preparation; preserve existing
  Work and code-mode semantics.
- Use the repository's fake Page/Locator pattern instead of adding an
  undeclared JSDOM/browser dependency.
- Capture locked, duplicate-switch, complete-citation, and network-inert eval
  fixtures.
- Choose smoke-test models from live status evidence instead of hardcoding
  Terra.
- Run type and module-graph gates throughout the task graph.
- Close failed recovery tabs and persist the actual recovered page URL.

## Revision Result

The TDD plan now addresses each blocking and major finding. The live
Perplexity DOM observation additionally replaces screenshot assumptions with
the observed composer, upload, model-row, Thinking, streaming, Copy, and
Sources-pane contracts.
